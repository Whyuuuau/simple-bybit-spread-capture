# Trading Bot Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
ccxt>=4.2.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Machine Learning
# XGBoost (RECOMMENDED - faster & better than LSTM)
xgboost>=2.0.0

# TensorFlow (for LSTM fallback)
tensorflow>=2.15.0
scikit-learn>=1.3.0

# Technical Analysis (optional but recommended)
TA-Lib>=0.4.28

# Note: TA-Lib installation on Windows requires binary wheels
# Download from: https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib
# Then install with: pip install TA_Lib-0.4.28-cp311-cp311-win_amd64.whl
# (adjust version for your Python version)

# If TA-Lib fails to install, the bot will fall back to pandas implementations
